---
name: remake-any-video
description: 逆向拆解一条视频的拍摄与制作方法并复刻它：给定一个视频链接，下载并抽取关键帧、字幕与音频，产出(1)分镜脚本、(2)可直接用于AI绘图/生图工具的制图提示词（含图生视频运镜提示）、(3)完整复刻/仿拍制作指南。当用户丢来一个视频网址（YouTube、B站/Bilibili、抖音、快手、小红书、TikTok、Instagram Reels等）并表达"分析这个视频""这个视频怎么拍/怎么做的""拆解一下这个视频""复刻/仿拍/重制这个视频""学习这个视频的运镜和风格"等意图，或英文 analyze/breakdown/reverse-engineer/remake/recreate this video 之类请求时，都要主动使用本Skill——即便用户只发了一个链接、没有说"拆解"两个字，只要意图是理解或复刻视频的制作方法而不是单纯想要内容摘要，就应触发。
---

# 视频逆向拆解与复刻

## 这个 Skill 在做什么，以及为什么这样设计

用户甩来一个视频链接，目的不是"看懂这条视频讲了什么内容"（那是普通的视频总结），而是"看懂这条视频
是怎么被做出来的"，然后能照着做一条类似的。这两件事需要的分析深度完全不同：内容总结关心信息，这个
Skill 关心**制作层面**——镜头怎么分、怎么运镜、光线怎么打、什么调色风格、剪辑节奏多快、每一帧要用
什么样的 AI 生图提示词才能重新画出来。

所以整个流程不是"看一遍视频凭印象写感想"，而是先把视频拆成真实存在的关键帧（用场景检测算法抽出来，
而不是脑补），逐帧做视觉分析，再综合成三份产出物。这样产出的分镜脚本时间码是精确到秒/帧的，制图
提示词是能直接丢进 Midjourney / 即梦 / 可灵 这类工具用的，而不是"一个咖啡店的镜头"这种空泛描述。

## 触发场景

用户发来视频链接并表达出"看懂/学会这条视频制作方法"的意图，哪怕只是甩一个链接加一句"这个怎么拍
的"、或者干脆只有链接没多说什么。不要把这类请求当成"帮我总结一下这个视频在讲什么"来处理——除非用户
明确说自己只是想要内容摘要而不关心制作方法。

## 环境依赖

本 Skill 需要一个**能执行 bash 脚本、能调用 yt-dlp 与 ffmpeg、并且能查看图片文件**的运行环境，
同时网络要能访问目标视频平台。具体依赖：

- **yt-dlp**：下载视频、拉取平台字幕/元数据。
  - Linux / macOS：`pip install yt-dlp --break-system-packages`
  - Windows：`pip install yt-dlp`（不需要 `--break-system-packages`）
- **ffmpeg / ffprobe**：抽帧、抽音频、读元数据。
  - macOS：`brew install ffmpeg`
  - Linux：`apt install ffmpeg`
  - Windows：`winget install Gyan.FFmpeg` 或 `scoop install ffmpeg`
- **网络能访问目标视频平台**（YouTube/B站/抖音/小红书等）。如果当前环境的网络策略屏蔽了这些域名，
  下载会失败——处理方式见下面"素材获取失败时怎么办"。
- **读图能力**：视觉分析靠的是宿主自带的多模态读图能力，打开抽出来的关键帧逐张看，不需要额外的
  视觉模型。如果宿主不支持读图，这个 Skill 无法给出可信的镜头分析——不要凭文件名或时长瞎编画面。

## 整体工作流程

### 第一步：确认目标与复刻模式

默认走完下面七步，产出完整三份交付物。如果用户措辞明显只要其中一部分（比如"只要分镜脚本"），就只
产出那部分，但抽帧和逐帧分析这两步仍然要做——它们是所有产出的地基，不能跳过直接凭空写。

如果视频较长（超过 3 分钟），开工前跟用户确认一下：整支都拆，还是只拆某个片段（比如广告/短视频的
黄金开场几秒，分析价值往往远高于中后段）。视频越长逐帧分析工作量越大，没必要死磕全片。

#### 必须问：是否做变换（不可跳过）

当用户的目标包含"**复刻**"（不只是分析）时，**在写任何提示词之前必须问一次：要不要做角色替换／
动作迁移／音色替换**。用结构化提问工具给出可多选选项，并把默认项摆出来：

| 选项 | 含义 |
|---|---|
| **都不做 —— 1:1 完整复刻（默认）** | 原角色 / 原场景 / 原动作 / 原音频，逐镜照搬 |
| 角色替换 | 换画面里的角色，保留动作、镜头、场景、台词 |
| 动作迁移 | 换角色的动作，保留角色、场景、镜头 |
| 音色替换 | 换配音音色，保留台词内容、时间码、画面 |
| 场景替换 | 换场景环境，保留角色、动作、镜头 |

**默认永远是"原角色、原场景、原动作、原音频、1:1 完整复刻"。** 用户没回答或回答含糊时，一律按
默认执行，并在交付说明开头写明"按默认 1:1 复刻执行；如需变换请告知"。

确定后把结果写进交付物开头的**复刻模式**一行，后续所有资产、描述、提示词都以此为准。

各项的定义、需要的输入、技术路径、验证要点与组合冲突见 `references/optional_operations.md`。
**多项同时做时强烈建议拆成多轮**——每轮只加一个变量，否则一次生成很难验收。

### 第二步：抓取素材

用 `scripts/extract_shots.sh` 一次性搞定下载、抽帧、抽字幕、抽音频：

```bash
bash scripts/extract_shots.sh -u "<视频链接>" -o ./analysis
```

跑完会得到：
- `analysis/frames/shot_XXXX.jpg` —— 按场景切换抽出的关键帧，已包含开场第一帧
- `analysis/frames/timestamps.txt` —— 每张关键帧对应的精确时间戳（秒），按行号与文件序号一一对应，
  分镜脚本的时间码全部从这里取，不要自己估
- `analysis/audio/audio.mp3` —— 完整音轨，用来判断配乐风格、卡点、音效
- `analysis/subs/` —— 平台字幕（若有），是台词/文案最准的来源，比自己听音频转写靠谱
- `analysis/meta/` —— 分辨率、帧率、时长等元数据

**如果关键帧数量明显偏少**（整条视频只切出一两张图，但时长有大几十秒）：大概率是里面有慢速推拉摇移
镜头没有触发"场景切换"判定，不是真的只有一两个镜头。加上 `-i` 参数按固定间隔补抽（例如 `-i 2` 每
2 秒一帧），交叉参考着用。反过来如果切出的帧多到离谱（几百张），把 `-t` 调高（比如 0.4）降低灵敏度
再跑一次。

**如果素材获取失败**（下载失败、无字幕、平台有反爬或登录墙），按优先级降级处理：
1. 直接问用户能不能把视频文件发过来，或者截几张关键画面发过来；
2. 搜索一下这条视频有没有被写过深度拆解、影评、幕后花絮之类的文章作为辅助信息源（但视觉分析终究要
   靠真实画面，文字描述不能替代，不要拿别人的文字描述当成自己看过画面来写分析）；
3. 至少把标题、简介、评论区相关信息抓到，能多提取一点是一点，然后如实告诉用户素材不全、分析会打
   折扣，绝不能在没看到画面的情况下凭空编造镜头内容。

### 第三步：搭建镜头清单骨架

对着 `frames/` 目录和 `timestamps.txt`，先搭一个空的分镜表骨架（镜号 + 时间码 + 时长），景别/运镜/
描述这些列留空，下一步再填。这样能先对总镜头数、整体节奏有个数——这条视频总共切了几刀、平均每镜几
秒——分析每一帧时才有参照系，不是孤立地看每张图。

### 第四步：逐镜头视觉分析（先看运动，再写描述）

#### 4a. 先看运动（**不可跳过**）

**单张关键帧说明不了"发生了什么"。** 动作、因果、说话人都存在于**帧与帧之间**：

- 主体从哪里来、做了什么、到哪里去
- 有没有主体在镜头中途进入或离开画面
- **谁在说话**——这取决于**口型**，静帧看不出来

先用 `scripts/extract_shot_frames.py` 按镜头抽多帧、做成"动作条"，逐条看：

```bash
python scripts/extract_shot_frames.py --video input.mp4 --plan plan.json \
    --out ./shots --per-shot 4 --dialogue ./subs/timing.csv --dialogue-frames 4
```

产出 `strips/shot_NNN.png`（每个镜头横排 4 帧）与 `dialogue/dlg_NNN.png`（每条字幕内抽几帧，
用来判断谁在动嘴）。**短镜（<1 秒）提到 6 帧，长镜（>8 秒）提到 6~8 帧**，否则中段转折会漏掉。

**翻车案例（务必避免）**：某次复刻全程只用"每镜一张静帧"写提示词，结果原片第 2 个镜头
「噜噜**追蝴蝶**、**掉进树洞里**」的整个情节在成片里完全不存在——因为锚点帧只拍到"噜噜在巨树前
的背影"，追蝴蝶与掉落都是帧间事件，静帧里没有。同时大量本该蘑菇说的台词被安到了噜噜头上，
因为没人看口型。**只看静帧，这两类错误都是必然的。**

对每条动作条写**动作弧**：起始状态 → 中间运动 → 结束状态，外加两个关键字段：

- **事件**：这一段**发生了什么**（追、掉、跑、抱、递、转身…）。这是动作弧的灵魂；
  写成"巨树前的森林空地"是画面，写成"追蝴蝶时失足掉进树洞"才是事件
- **入场/出场**：有没有主体在中途进入或离开画面

#### 4b. 判断说话人（口型法，**不许猜**）

用 `dialogue/dlg_NNN.png`：

1. **口型比对**：字幕时间段内，说话者的嘴在帧间有**明显开合形变**，听者基本不动
2. **语义交叉验证**：问句之后紧接着的那句，多半是**另一个人**说的
3. **画外音**：全程没有角色动嘴 → 标 `off-screen`，不要硬安到画面里的角色身上
4. **一句内换人**：嘴部形变先出现在 A 后出现在 B → 中途换了说话人

判断不了就如实标"说话人待确认"，**不要编**——安错人会让整段对白的音画关系全错。

#### 4c. 再写静态维度

把 `frames/` 里的关键帧逐张打开查看，每一帧都落实下面这些维度。术语从 `references/shot_glossary.md`
里挑，不要自己造词——这样全片描述用词统一，而且能直接喂给生图工具：

- **景别**：特写/近景/中景/全景/远景/大远景
- **拍摄角度**：平视/仰拍/俯拍/顶拍/斜角
- **运镜**：固定/摇/推拉/变焦/跟拍/手持/稳定器/空中/环绕/甩镜——**运镜必须结合 4a 的多帧观察来定**，
  不要只看单帧瞎猜
- **构图**：三分法/对称/引导线/框架式/前景遮挡/负空间/浅景深
- **光线**：自然光/硬光软光/顺光逆光侧光/色温冷暖/剪影
- **色彩与调色风格**：主色调、饱和度、对比度，偏电影感青橙调、复古胶片、高饱和电商风、日系清新、
  赛博朋克霓虹中的哪一种
- **画面主体与动作**：谁/什么在画面里，在做什么（**与 4a 的动作弧对齐**）
- **文字/图形**：有没有字幕条、标题、贴纸、UI元素，字体风格、位置、动画方式
- **转场**：和下一镜怎么衔接——硬切/淡入淡出/叠化/匹配剪辑/遮挡转场/跳切

把这些内容填回第三步搭好的骨架。

方法细节、台词分配表与检查清单见 `references/keyframe_sequence_description.md`。

**但逐帧描述只完成了一半。** 逐帧描述回答的是"这一帧长什么样"；视频生成需要的是"**从这一帧到下一帧发生了什么**"。只交逐帧描述，生成出来就是两张幻灯片硬拼——角色位置对不上、光线跳变、动作不接续、对话接不上。

所以关键帧描述要做成**三层**：

1. **资产库**：为每个关键**角色、服装、道具、场景**各建一条资产——**一张参考图 + 一段固定描述 + 一个固定 ID**（参考图优先直接从源片最清晰、遮挡最少的那一帧裁出来，且**必须避开字幕**）。另建一条全片**风格**资产。
   - **服装必须单独建条目**：它是跨镜最容易漂移的一项（同一个人换镜后衣服变了，观众立刻察觉）
   - **道具也要单独建**：剧情里起作用的关键物件换了样子同样刺眼
   - **有声角色必须建「音色」资产**：音视频联合生成的模型**每段独立采样**——不锚定音色，同一个角色在不同段会像两个人（实测跨段音色差异达源片基线的 **18 倍**）。源片有音轨时按角色从原声抽出干净样本，用 `<Audio N>` 音色参考锚定；源片无音轨才退而用 TTS 合成。方法与两个必避的坑见 `references/voice_consistency.md`
   - ⚠️ **封装工具的槽位类型可能更少**。例如某导演台只支持 `角色/场景/道具/分镜/其他`——**没有「服装」**。此时服装在**引用**时挂到 `角色` 或 `其他` 槽位，但**资产库里仍单独建条目**，用于逐镜核对一致性
2. **逐帧状态描述**：景别、角度构图、**主体状态（谁·在画面哪个位置·朝向·姿态·表情）**、环境、画面文字、**引用资产**。
3. **相邻帧过渡与动作弧**（核心）：起始状态 → **事件（这一段到底发生了什么）** → **中间运动（主体位移与动作 + 镜头类型/速度/幅度）** → 结束状态，外加镜头内**入场/出场**、对话（说话人+内容+起止时刻）、**引用资产**与**不变量**（过渡期间必须保持不变的元素）。

**只写统一的文字描述是不够的——它只能统一"措辞"，给不了模型视觉锚点。** 模型每一帧都是独立生成的，没有共同的图像参照，同一件衣服照样会变成三种样子。所以**每一条提示词（逐帧的与过渡的都要）都必须显式声明引用了哪些资产**，并同时复述资产描述——图像锚点负责"像不像"，文字描述负责"别漏掉特征"。只在开头统一说一次"角色见资产库"没有用：提示词是单独送去生成的，模型看不到上下文。

写法要点、常见错误与装配检查清单见 `references/keyframe_sequence_description.md`。**原片在相邻两帧之间切了镜的，必须标注硬切并拆成两条过渡描述**——糊成一条连续运动会让模型把两个空间叠在一起。

### 第五步：节奏、结构与音频分析

看完所有关键帧后退一步看整体：

- **剪辑节奏**：平均镜头时长、每分钟切了多少刀——快节奏（1~2秒/镜）常见于电商/信息密度高的内容，
  慢节奏（3~5秒以上/镜）常见于氛围感/情绪类内容
- **叙事结构**：有没有"钩子(前3秒)→展开→高潮/转折→收尾(CTA或余韵)"这种结构，短视频/广告类内容
  里几乎是标配
- **音乐/音效**：听一遍 `audio/audio.mp3` 判断音乐风格、大致 BPM 快慢，有没有明显的"卡点"（剪辑
  点是否踩在音乐鼓点或旋律转折上）
- **台词/文案**：从 `subs/` 字幕或画面文字获取，整理出完整文案

### 第六步：规划生成分段（要真机生成时才做）

如果用户的目标不只是"分析"，而是**要用视频模型真机生成一条复刻片**，那么反推出的镜头清单**不能直接
拿去生成**——视频模型单段有固定的时长约束，必须先把镜头规划成"生成分段"。

**绝对不要按固定时长均匀切。** 均匀切在快剪视频上会彻底破坏结构：假设源片 92.9 秒、34 个镜头（平均
2.7 秒/镜），若均匀切成 21 段 × 4.46 秒，每段实际跨了 2.57 个镜头，于是每段只渲染"锚点那一帧"的场景
并撑满整段——结果是**复刻保留了原片已经切走的画面、同时丢掉了切进来的画面**，观感与源片明显不同。

正确做法（**默认策略**）：

1. **段的边界落在源片真实切点上**，不用均匀间隔
2. **尽可能一个镜头对应一个分段**
3. 单镜头**短于模型最短段长** → 与相邻镜头**合并**
4. 单镜头**长于模型最长段长** → **拆分**（均分为多段、同场景延续）
5. 每段时长取该段源码跨度的**最近档位**；**锚点帧取该段第一镜的起始帧**（不是段中点）
6. 一段若含多个镜头，提示词要写成**一个连续镜头内的运动推进**，不要并列描述几个独立画面

直接用 `scripts/plan_segments.py` 生成分段表：

```bash
python scripts/plan_segments.py --video input.mp4 --out plan.json
```

它会自动做场景检测、按上述规则分组、算出每段时长档位与锚点时间，打印分段表并写出 JSON。

**必须向用户交代的取舍**：源片镜头数与模型最短段长往往对不上，两者只能取其一——默认的
`--policy keep-duration` 保总长（短镜合并）；`--policy keep-shots` 保镜头数但总长会明显拉长。
开工前讲清楚让用户选，不要默默替他决定。

各模型的时长档位表、算法细节与输出格式见 `references/generation_segmentation.md`。

### 第七步：生成三份交付物

产出前先看一眼 `references/output_templates.md` 里那个完整跑通的范例，照同等详细程度写，不要比它
更粗略。三份东西分别是：

**A. 分镜脚本**（表格，逐镜头）：

| 镜号 | 时间码 | 时长 | 景别 | 运镜 | 画面描述 | 台词/字幕/文案 | 音乐音效 | 转场 |
|---|---|---|---|---|---|---|---|---|

**B. 制图提示词**（每个关键镜头一组，中文说明 + 英文Prompt——主流生图/生视频工具对英文理解最准）：

```
镜头0X｜[一句话中文描述]
生图 Prompt（文生图）：[主体], [景别+角度], [构图], [光线], [色彩氛围], [风格参考], [画质关键词], --ar [比例]
运镜 Prompt（图生视频，以上图为首帧）：[主体动作], [镜头运动], [节奏], [建议时长]
```

**C. 复刻指南**（两条路径都给，不要只给一种）：
- 路径 A：AI 生成流程（B 里的提示词怎么串联——生图→生视频→剪辑的具体步骤）
- 路径 B：实拍流程（设备、场地、拍摄顺序、每个镜头的执行要点）
- 后期制作（剪辑软件、调色方向、字幕样式、音乐音效来源、导出参数）
- 平台适配（目标平台的画幅/时长/黄金开场秒数等要求）

#### 生成提示词必须**中英两份**

真正提交生成的那份是**严格按目标模型官方规范**写的版本（对 MiniMax H3 就是隔壁
`h3-prompt-writing` 技能里的全参考模式规范文件 `../h3-prompt-writing/references/ref-en.txt`：
六个 section 全英文、只有 `<d>` 内对白保留原语言）。但用户看不懂英文就没法复核，
所以**同时给一份中文对照译本**：

```
prompts/
├── H3提示词_EN.txt     ← 提交生成用（严格按官方规范）
└── H3提示词_CN.txt     ← 人工复核用（逐段对照译本，不用于提交）
```

两条规矩：

1. **英文版是唯一提交给模型的版本**；中文版只是译本，**不要**把它当成生成输入
2. 用户复核后要改内容时，**改完必须同步回英文版**，否则两份会不一致——
   改完重新出 `产出物清单.md` 再确认

**源片带字幕时**，字幕必须单独成章：**不要指望视频模型把中文字幕画对**——正确做法是提取文本与
时间码、在后期按原片样式重新叠加。提取用 `scripts/extract_subtitles.py`，完整流程见
`references/subtitle_replication.md`。

**并且字幕只产出「一个文件」**（`scripts/build_subtitle_asset.py`，输出 `.ass`）。
以前会同时产出 `.json`/`.csv`/`.md`/`.ass` 四份，改一次要同步四处，**极易出现
"改了 json 忘了 ass"这类隐患**。现在一份 `.ass` 同时满足三个用途：人看（记事本可读）、
机器读（格式固定）、直接可用（`ass=` 滤镜可烧录）。字段承载：

- `Name` 字段 = **说话对象**；`Text` = 台词；`Start/End` = 精确时间；`[V4+ Styles]` = 样式
- 每条紧邻的 `;` 注释行 = **语速下限 / 语气 / 检出次数 / 所属生成段**
  （`;` 是 ASS 标准注释语法，libass 会忽略，不影响渲染）

两条硬规则：

1. **同一句在多次检测中重复出现时，以第一次出现的时间为准**并合并成一条。
   说话慢或台词长时同一句会横跨很多采样帧，采样抖动还会把它断成几条；不合并就会出现
   "同一句被说好几遍"这类 bug
2. **说话对象必须来自口型判断**（`extract_shot_frames.py --dialogue` 抽出的对白条），
   不能靠"谁在画面里"猜；判断不了就填「待确认」，不要编

语速一栏只能当**下限**看（字幕停留时长 ≥ 实际说话时长）；算出来明显偏低时**先怀疑转录漏字**。

**复刻成片总长通常与源片不同**，所以字幕时间不能整体缩放，要按**分段边界做逐段线性映射**
（源片 `[a,b]` 对应成片 `[ra,rb]`，段内按比例插值），用 `--time-map` 传入映射表。

格式拿不准的地方直接照 `references/output_templates.md` 里的结构来，不要另起一套。

## 质检门禁

这条流水线上各道工序的代价差几个数量级：抓素材几分钟，**真机生成每段几十分钟、全片数小时**。所以原则是**每道工序产出后立即验收，不合格绝不进入下一道**——把问题拦在便宜的那一侧。

| 门禁 | 工序 | 验收物 |
|---|---|---|
| **G0** | 抓素材 | `frames/` `timestamps.txt` `audio/` `subs/` `meta/` |
| **G1** | 镜头清单 | 镜号 / 时间码 / 时长 |
| **G1b** | **动作与说话人** | 镜头动作条 + 动作弧 + 台词分配表（只看静帧必然丢动作与说话人） |
| **G2** | **资产库** | 资产库表 + 资产引用表（最关键：错了后面每段都错） |
| **G3** | 序列描述 | 三层描述完整性与写法质量 |
| **G4** | 分段规划 | `plan.json`：档位换算、覆盖率、总长偏差 |
| **G5** | 交付物 | 三份交付物 + **生成提示词副本** |
| **G5b** | **提交生成之前** | **产出物清单 + 用户人工复核确认**（不可跳过） |
| **G6** | **试跑** | **先跑 1~3 段验收，再决定是否全片**（最省时间的一条规则） |
| **G7** | 成片 | 分辨率/音画对齐/字幕/段间接缝 |

### G5b：生成前必须让用户人工复核（不可跳过）

**在提交生成任务之前**，先产出清单让用户指着清单复核：

```bash
python scripts/build_manifest.py --project <项目目录> --source <源片路径>
```

它会在项目目录生成 `产出物清单.md`，列出所有输入产出并**逐条写明复核要点**。三条硬规则：

1. **复核相关的东西必须落在项目目录内**（不能在工具的 input 目录或临时目录里）——否则用户看不到、也就无从复核。特别是**生成提示词**：它是决定生成结果的核心输入，却最容易只留在工具侧
2. **必须用结构化提问工具问用户**，不能自行假定"应该没问题"。确认至少覆盖三类：**字幕文本**（逐字核对，放大的 `字幕/lines/` 图是可读版本）、**关键帧/分镜锚点**、**生成提示词**。选项给足 5 个有实质差异的项，推荐项放第一位
3. **用户没有明确表示"通过"之前，不要提交生成任务**；回答含糊时按最保守的选项处理（先跑试跑段），并在交付说明里写明

**为什么必须由人看这三样**：脚本只给字幕**图**不给文字，转录靠人眼，而相邻重复字（如「不能不要」）
极易读漏、漏字后句子往往仍通顺（语感校验失效）；锚点决定构图；提示词决定内容与说话人。
这三样任何一样错了，代价都是整轮 GPU 时间。

逐项要点见 `references/qc_checklist.md` 的 G5b。

能自动查的项用脚本查，不要人工看：

```bash
python scripts/qc_check.py --analysis ./analysis --plan plan.json --script 剧本.txt
```

它会按 G0~G5 输出 PASS / WARN / FAIL，并给出处置提示；退出码非 0 表示存在 FAIL。

**G6 试跑门是效率关键**：全片生成前必须跑 1~3 段（覆盖角色特写、多镜头段、最复杂场景各一个），验收构图遵循度、角色一致性、结构缺陷、时长。判定参考——构图归一化互相关 **> 0.8 优秀、> 0.5 可接受、< 0.3 说明没跟随参考帧**。不合格就**停下改上游**，不要在明知不合格时继续烧 GPU。

逐项检查内容、阈值依据与常见错误的处置建议见 `references/qc_checklist.md`。

## 特殊情况

- **原视频出现真人清晰面部、可识别品牌/商标、或有版权的原创音乐**：如实拆解不受影响，但在复刻指南
  里提醒用户——直接照搬这些元素做二次传播会有肖像权/商标/音乐版权风险。AI 路径建议生成虚构人物代替、
  实拍路径建议找自己的演员/场地拍摄、音乐建议换成曲风相似但可商用的曲库音乐。提一句就够，不用大段
  说教。
- **横屏还是竖屏**：照抓取到的原始比例分析和输出，制图提示词里的画幅比例参数要和原视频一致，不要
  默认套 9:16 或 16:9。
- **视频里有多段明显不同风格**（比如前半段产品实拍、后半段切成动画/图形）：分段说明，不要用一套
  风格描述硬套全片。

## 参考文件

- `references/shot_glossary.md` —— 第四步逐帧分析时查，镜头语言中英对照表，保证描述用词专业统一，
  且能直接喂给生图工具
- `references/keyframe_sequence_description.md` —— 第四步 4a/4b 必查：**为什么只看静帧必然丢动作与说话人**、
  动作弧写法、口型法判断说话人、台词分配表（防重复）与检查清单
- `references/optional_operations.md` —— 第一步门控询问后查：角色替换／动作迁移／音色替换的定义、
  需要的输入、技术路径、验证要点、组合冲突，以及默认的 1:1 完整复刻包含什么
- `references/keyframe_sequence_description.md` —— 第四步写关键帧描述时查：**资产库（参考图+固定描述+ID）
  与逐条引用规则**、逐帧状态、**相邻帧过渡描述**三层写法，常见错误与装配检查清单
- `references/generation_segmentation.md` —— 第六步规划生成分段时查：各视频模型的时长档位表、分段
  算法、多镜头段如何用"指定帧号锚定多个关键帧 + 分镜时间轴"生成、必须向用户交代的取舍
- `references/subtitle_replication.md` —— 源片带字幕时查：字幕类型判断、文本与时间码提取、样式
  分析清单、后期用 ASS 重新叠加的完整流程
- `references/output_templates.md` —— 第七步产出交付物时查，三份东西的完整模板 + 一个从头到尾跑
  通的范例
- `references/voice_consistency.md` —— 有声角色必查：音色为什么会跨段漂移、`<Audio N>` 音色参考怎么挂、两个必避的坑（`<Subject N>` 不一定是 S1/S2；说话人标记常在前一句里）、核验指标与判断阈值
- `references/text_free_references.md` —— **G2 资产库门必查**：参考图里的烧录字幕/常驻标题会被模型照抄成乱码。怎么用坐标网格量准文字范围、用邻域内容糊掉、后期再用同一个 .ass 把标题加回来，以及"对静图测字幕永远只显示第一条"这个坑
- `references/qc_checklist.md` —— 每道工序产出后查：G0~G7 门禁的逐项检查内容、判定阈值、
  不合格的处置建议，以及效率守则与快速止损信号
- `scripts/build_manifest.py` —— **提交生成之前**用它产出 `产出物清单.md`：把所有输入产出
  （含**生成提示词副本**）列成一张可复核的表，逐条写明复核要点，作为 G5b 用户确认的入口
